
# init Method
This method is used to initialize the SDK with the API Key.

## Parameters
- `init` - API Key received from Okto.

```tsx copy
init(
    apiKey: string,    
) => void;
```

## Example
```tsx copy
import { init } from 'rn-okto-sdk';

const API_KEY = ""
init(API_KEY);
```
